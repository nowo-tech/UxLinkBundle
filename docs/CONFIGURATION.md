# Configuration

```yaml
# config/packages/nowo_ux_link.yaml
nowo_ux_link:
    defaults:
        target: '_blank'
        rel: 'noopener noreferrer'
        show_icons: true
    aliases:
        share:
            twitter: x
```

See `Configuration.php` for the full tree.

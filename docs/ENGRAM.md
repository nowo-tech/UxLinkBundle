# Engram (MCP)

Product specification and `REQ-*` traceability: [Spec-driven development](SPEC-DRIVEN-DEVELOPMENT.md).

## Cursor / IDE

- File: `.cursor/mcp.json`
- Server: `engram` with command `engram` and arguments `["mcp"]`

Install the Engram CLI locally to enable MCP. The bundle works without it.
